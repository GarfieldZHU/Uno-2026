# Development

Run `cargo test -p uno-core`, `npm run typecheck`, `npm run build`, and
`npm run test:browser`. Rules belong in Rust; UI changes belong in React/CSS;
public contract changes update both languages. Read `AGENTS.md` before using an
agent. See [`docs/DEVELOPMENT.md`](../docs/DEVELOPMENT.md).
