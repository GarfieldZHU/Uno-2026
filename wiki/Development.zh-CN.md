# 开发

运行 `cargo test -p uno-core`、`npm run typecheck`、`npm run build` 和
`npm run test:browser`。规则写在 Rust，UI 写在 React/CSS，公共契约变化同步两种语言。
使用 Agent 前先读 `AGENTS.md`。详见 [`docs/DEVELOPMENT.zh-CN.md`](../docs/DEVELOPMENT.zh-CN.md)。
