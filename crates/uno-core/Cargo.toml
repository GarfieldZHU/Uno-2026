[package]
name = "uno-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Deterministic UNO rules and AI shared by native Rust and WebAssembly."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
wasm-bindgen = "0.2"

[dev-dependencies]
